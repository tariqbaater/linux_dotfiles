-- Omarchy Keybind Manager window presentation.
-- This user rule intentionally inherits the active Omarchy theme's borders,
-- rounding, blur, and animation settings.
o.window("^org\\.omarchy\\.keybind-manager$", {
  float = true,
  size = { 760, 560 },
  center = true,
})
