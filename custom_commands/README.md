# Custom Binary Commands

These are my custom binary commands that I copied into /usr/bin, I use `nvims` for nvim config picker
and others are `on` for obsidian new note creation from the command line and `og` to automatically organize
notes in zettelkasten into their respective tags.
